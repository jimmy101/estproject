<h1><div align ="center">Agent Information</div></h1>
    <dt>   
	
<table>
  <tr>
    <th>First name</th>              
    <th>Last name</th>
    <th>Email</th>
    <th>Phone</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @agents.each do |agent| %>
  <tr>
    <td><%= agent.first_name %></td>
    <td><%= agent.last_name %></td>
    <td><%= agent.email %></td>
    <td><%= agent.phone %></td>
    <td><%= link_to 'Show', agent %></td>
    <td><%= link_to 'Edit', edit_agent_path(agent) %></td>
    <td><%= link_to 'Destroy', agent, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Agent', new_agent_path %> <br><br>
<%= link_to 'home page', properties_path %> 
